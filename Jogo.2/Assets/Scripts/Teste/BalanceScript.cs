using UnityEngine;

public class BalanceScript : MonoBehaviour
{
    public Transform balanceBoard;// Balance 
    public Transform balanceTopLeft;// Balance 
    public Transform balanceTopRight;// Balance 
    public Transform balanceBottomLeft;// Balance 
    public Transform balanceBottomRight;// Balance 
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
